package dev.rnap.reactnativeaudiopro

import org.mockito.kotlin.mock
import org.mockito.kotlin.whenever

// Mock classes for React Native dependencies
interface WritableMap

/**
 * Test utilities for AudioPro tests
 *
 * Note: This is a simplified version that uses mock classes instead of actual dependencies
 */
object AudioProTestUtils
